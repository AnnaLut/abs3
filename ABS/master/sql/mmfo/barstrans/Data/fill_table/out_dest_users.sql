MERGE INTO OUT_DEST_USERS A USING
 (SELECT
  'MMFO_ABSADM01' as USERNAME,
  'Basic' as AUTH_TYPE,
  'YWJzYWRtMDE6cXdlcnR5' as AUTH_STR,
  2009401 as USER_ID
  FROM DUAL) B
ON (A.USERNAME = B.USERNAME)
WHEN NOT MATCHED THEN 
INSERT (
  USERNAME, AUTH_TYPE, AUTH_STR, USER_ID)
VALUES (
  B.USERNAME, B.AUTH_TYPE, B.AUTH_STR, B.USER_ID)
WHEN MATCHED THEN
UPDATE SET 
  A.AUTH_TYPE = B.AUTH_TYPE,
  A.AUTH_STR = B.AUTH_STR,
  A.USER_ID = B.USER_ID;

MERGE INTO OUT_DEST_USERS A USING
 (SELECT
  'MMFO_ABSADM11' as USERNAME,
  'Basic' as AUTH_TYPE,
  'YWJzYWRtMTE6cXdlcnR5' as AUTH_STR,
  2009411 as USER_ID
  FROM DUAL) B
ON (A.USERNAME = B.USERNAME)
WHEN NOT MATCHED THEN 
INSERT (
  USERNAME, AUTH_TYPE, AUTH_STR, USER_ID)
VALUES (
  B.USERNAME, B.AUTH_TYPE, B.AUTH_STR, B.USER_ID)
WHEN MATCHED THEN
UPDATE SET 
  A.AUTH_TYPE = B.AUTH_TYPE,
  A.AUTH_STR = B.AUTH_STR,
  A.USER_ID = B.USER_ID;

COMMIT;
