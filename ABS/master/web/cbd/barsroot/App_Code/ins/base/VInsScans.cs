/*
    AUTOGENERATED! Do not modify this code.
*/

using System;
using System.Collections;
using System.Collections.Generic;
using System.Text;
using System.Collections.Specialized;
using System.Data;
using System.Web.Configuration;
using Oracle.DataAccess.Client;
using Oracle.DataAccess.Types;
using ibank.core;
using Bars.Classes;

namespace Bars.Ins
{
    public sealed class VInsScansRecord : BbRecord
    {
        public VInsScansRecord(): base()
        {
            fillFields();
        }
        public VInsScansRecord(BbDataSource Parent) : base (Parent)
        {
            fillFields();
        }
        public VInsScansRecord(BbDataSource Parent, OracleDecimal RowScn, String SCAN_ID, String NAME)
            : this(Parent)
        {
            this.SCAN_ID = SCAN_ID;
            this.NAME = NAME;
            this.RowScn = RowScn;
            this.IsRowscnSupported = false;
            this.ClearChanges();
        }
        private void fillFields()
        {
            Fields.Add( new BbField("SCAN_ID", OracleDbType.Varchar2, false, false, false, false, false, "V_INS_SCANS", ObjectTypes.View, "Сканкопії страхового договору (Представлення)", "Ідентифікатор"));
            Fields.Add( new BbField("NAME", OracleDbType.Varchar2, false, false, false, false, false, "V_INS_SCANS", ObjectTypes.View, "Сканкопії страхового договору (Представлення)", "Найменування"));        
        }
        public String SCAN_ID { get { return (String)FindField("SCAN_ID").Value; } set {SetField("SCAN_ID", value);} }
        public String NAME { get { return (String)FindField("NAME").Value; } set {SetField("NAME", value);} }
    }

    public sealed class VInsScansFilters : BbFilters
    {
        public VInsScansFilters(BbDataSource Parent) : base (Parent)
        {
            SCAN_ID = new BBVarchar2Filter(this, "SCAN_ID");
            NAME = new BBVarchar2Filter(this, "NAME");
        }
        public BBVarchar2Filter SCAN_ID;
        public BBVarchar2Filter NAME;
    }

    public partial class VInsScans : BbTable<VInsScansRecord, VInsScansFilters>
    {
        public VInsScans() : base(new BbConnection())
        {
            // base.Connection.RoleName = "WR_WCS";
        }
        public VInsScans(BbConnection Connection)
            : base(Connection)
        {
        }
        public override List<VInsScansRecord> Select(VInsScansRecord Item)
        {
            List<VInsScansRecord> res = new List<VInsScansRecord>();
            OracleDataReader rdr = null;
            ConnectionResult connectionResult = Connection.InitConnection();
            try
            {
                rdr = ExecuteReader(Item);
                while (rdr.Read())
                {
                    res.Add(new VInsScansRecord(
                        this,
                        rdr.IsDBNull(0) ? OracleDecimal.Null : rdr.GetOracleDecimal(0),
                        rdr.IsDBNull(1) ?  (String)null : Convert.ToString(rdr[1]), 
                        rdr.IsDBNull(2) ?  (String)null : Convert.ToString(rdr[2]))
                    );
                }
            }
            finally
            {
                DisposeDataReader(rdr);
                if (ConnectionResult.New == connectionResult)
                    Connection.CloseConnection();
            }
            return res;
        }
    }
}