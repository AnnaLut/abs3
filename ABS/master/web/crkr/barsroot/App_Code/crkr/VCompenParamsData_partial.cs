/*
    AUTOGENERATED! Do not modify this code.
*/

using System;
using System.Collections;
using System.Collections.Generic;
using System.Text;
using System.Collections.Specialized;
using System.Data;
using System.Web.Configuration;
using Oracle.DataAccess.Client;
using Oracle.DataAccess.Types;
using ibank.core;
using Bars.Classes;

namespace Bars.CRKR
{
    public partial class VCompenParamsData
    {
        public List<VCompenParamsDataRecord> SelectByParamType(Decimal? TYPE, String PAR, String DAT, String STATUS, string SortExpression)
        {
            if (String.IsNullOrEmpty(SortExpression))
            {
                SortExpression = "ID DESC";
            }

            if (TYPE == null)
            {
                this.Filter.TYPE.Equal(1);
                return this.Select("PAR ASC");
            }
            else
            {
                this.Filter.TYPE.Equal(TYPE);

            }

            if (!String.IsNullOrEmpty(PAR))
            {
                this.Filter.PAR.Equal(PAR);
            }

            if (DAT == DateTime.MinValue.ToShortDateString().ToString())
            {
                this.Filter.DATE_FROM.LessEqual(DateTime.MaxValue);
                this.Filter.DATE_TO.GreaterEqual(DateTime.MinValue);
            }
            else if (!String.IsNullOrEmpty(DAT))
            {
                this.Filter.DATE_FROM.LessEqual(Convert.ToDateTime(DAT));
                this.Filter.DATE_TO.GreaterEqual(Convert.ToDateTime(DAT));
            }

            if (!String.IsNullOrEmpty(STATUS))
            {
                this.Filter.IS_ENABLE.Equal(Convert.ToDecimal(STATUS));
            }

            return this.Select(SortExpression);
        }
    }
}