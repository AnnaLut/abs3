SET DEFINE OFF;
begin
MERGE INTO BARS.NBU_BRANCH_601 A USING
 (SELECT
  '304665' as KF,
  'Луганське РУ' as BRANCH_NAME,
  1 as IS_INTERNAL,
  '*https://web-lugn.ho.obu/barsroot/' as SERVICE_BASE_URL,
  NULL as SERVICE_AUTH,
  1 as IS_ACTIVE
  FROM DUAL) B
ON (A.KF = B.KF)
WHEN NOT MATCHED THEN 
INSERT (
  KF, BRANCH_NAME, IS_INTERNAL, SERVICE_BASE_URL, SERVICE_AUTH, 
  IS_ACTIVE)
VALUES (
  B.KF, B.BRANCH_NAME, B.IS_INTERNAL, B.SERVICE_BASE_URL, B.SERVICE_AUTH, 
  B.IS_ACTIVE)
WHEN MATCHED THEN
UPDATE SET 
  A.BRANCH_NAME = B.BRANCH_NAME,
  A.IS_INTERNAL = B.IS_INTERNAL,
  A.SERVICE_BASE_URL = B.SERVICE_BASE_URL,
  A.SERVICE_AUTH = B.SERVICE_AUTH,
  A.IS_ACTIVE = B.IS_ACTIVE;

MERGE INTO BARS.NBU_BRANCH_601 A USING
 (SELECT
  '338545' as KF,
  'Тернопільське РУ' as BRANCH_NAME,
  0 as IS_INTERNAL,
  '*https://bars.ternopil.obu/barsroot/' as SERVICE_BASE_URL,
  NULL as SERVICE_AUTH,
  0 as IS_ACTIVE
  FROM DUAL) B
ON (A.KF = B.KF)
WHEN NOT MATCHED THEN 
INSERT (
  KF, BRANCH_NAME, IS_INTERNAL, SERVICE_BASE_URL, SERVICE_AUTH, 
  IS_ACTIVE)
VALUES (
  B.KF, B.BRANCH_NAME, B.IS_INTERNAL, B.SERVICE_BASE_URL, B.SERVICE_AUTH, 
  B.IS_ACTIVE)
WHEN MATCHED THEN
UPDATE SET 
  A.BRANCH_NAME = B.BRANCH_NAME,
  A.IS_INTERNAL = B.IS_INTERNAL,
  A.SERVICE_BASE_URL = B.SERVICE_BASE_URL,
  A.SERVICE_AUTH = B.SERVICE_AUTH,
  A.IS_ACTIVE = B.IS_ACTIVE;

MERGE INTO BARS.NBU_BRANCH_601 A USING
 (SELECT
  '305482' as KF,
  'Дніпропетровське РУ' as BRANCH_NAME,
  0 as IS_INTERNAL,
  '*https://web-dnpr.ho.obu/barsroot/' as SERVICE_BASE_URL,
  NULL as SERVICE_AUTH,
  0 as IS_ACTIVE
  FROM DUAL) B
ON (A.KF = B.KF)
WHEN NOT MATCHED THEN 
INSERT (
  KF, BRANCH_NAME, IS_INTERNAL, SERVICE_BASE_URL, SERVICE_AUTH, 
  IS_ACTIVE)
VALUES (
  B.KF, B.BRANCH_NAME, B.IS_INTERNAL, B.SERVICE_BASE_URL, B.SERVICE_AUTH, 
  B.IS_ACTIVE)
WHEN MATCHED THEN
UPDATE SET 
  A.BRANCH_NAME = B.BRANCH_NAME,
  A.IS_INTERNAL = B.IS_INTERNAL,
  A.SERVICE_BASE_URL = B.SERVICE_BASE_URL,
  A.SERVICE_AUTH = B.SERVICE_AUTH,
  A.IS_ACTIVE = B.IS_ACTIVE;

MERGE INTO BARS.NBU_BRANCH_601 A USING
 (SELECT
  '356334' as KF,
  'Чернівецьке РУ' as BRANCH_NAME,
  0 as IS_INTERNAL,
  '*https://barschvweb.oschadbank.ua/barsroot/' as SERVICE_BASE_URL,
  NULL as SERVICE_AUTH,
  0 as IS_ACTIVE
  FROM DUAL) B
ON (A.KF = B.KF)
WHEN NOT MATCHED THEN 
INSERT (
  KF, BRANCH_NAME, IS_INTERNAL, SERVICE_BASE_URL, SERVICE_AUTH, 
  IS_ACTIVE)
VALUES (
  B.KF, B.BRANCH_NAME, B.IS_INTERNAL, B.SERVICE_BASE_URL, B.SERVICE_AUTH, 
  B.IS_ACTIVE)
WHEN MATCHED THEN
UPDATE SET 
  A.BRANCH_NAME = B.BRANCH_NAME,
  A.IS_INTERNAL = B.IS_INTERNAL,
  A.SERVICE_BASE_URL = B.SERVICE_BASE_URL,
  A.SERVICE_AUTH = B.SERVICE_AUTH,
  A.IS_ACTIVE = B.IS_ACTIVE;

MERGE INTO BARS.NBU_BRANCH_601 A USING
 (SELECT
  '326461' as KF,
  'Миколаївське РУ' as BRANCH_NAME,
  0 as IS_INTERNAL,
  '*https://web-mklv.ho.obu/barsroot/' as SERVICE_BASE_URL,
  NULL as SERVICE_AUTH,
  0 as IS_ACTIVE
  FROM DUAL) B
ON (A.KF = B.KF)
WHEN NOT MATCHED THEN 
INSERT (
  KF, BRANCH_NAME, IS_INTERNAL, SERVICE_BASE_URL, SERVICE_AUTH, 
  IS_ACTIVE)
VALUES (
  B.KF, B.BRANCH_NAME, B.IS_INTERNAL, B.SERVICE_BASE_URL, B.SERVICE_AUTH, 
  B.IS_ACTIVE)
WHEN MATCHED THEN
UPDATE SET 
  A.BRANCH_NAME = B.BRANCH_NAME,
  A.IS_INTERNAL = B.IS_INTERNAL,
  A.SERVICE_BASE_URL = B.SERVICE_BASE_URL,
  A.SERVICE_AUTH = B.SERVICE_AUTH,
  A.IS_ACTIVE = B.IS_ACTIVE;

MERGE INTO BARS.NBU_BRANCH_601 A USING
 (SELECT
  '354507' as KF,
  'Черкаське РУ' as BRANCH_NAME,
  0 as IS_INTERNAL,
  '*https://barsweb.cherkasy.obu/barsroot/' as SERVICE_BASE_URL,
  NULL as SERVICE_AUTH,
  0 as IS_ACTIVE
  FROM DUAL) B
ON (A.KF = B.KF)
WHEN NOT MATCHED THEN 
INSERT (
  KF, BRANCH_NAME, IS_INTERNAL, SERVICE_BASE_URL, SERVICE_AUTH, 
  IS_ACTIVE)
VALUES (
  B.KF, B.BRANCH_NAME, B.IS_INTERNAL, B.SERVICE_BASE_URL, B.SERVICE_AUTH, 
  B.IS_ACTIVE)
WHEN MATCHED THEN
UPDATE SET 
  A.BRANCH_NAME = B.BRANCH_NAME,
  A.IS_INTERNAL = B.IS_INTERNAL,
  A.SERVICE_BASE_URL = B.SERVICE_BASE_URL,
  A.SERVICE_AUTH = B.SERVICE_AUTH,
  A.IS_ACTIVE = B.IS_ACTIVE;

MERGE INTO BARS.NBU_BRANCH_601 A USING
 (SELECT
  '322669' as KF,
  'Київське РУ' as BRANCH_NAME,
  1 as IS_INTERNAL,
  '*https://mmfo-web.oschadbank.ua/barsroot/' as SERVICE_BASE_URL,
  NULL as SERVICE_AUTH,
  1 as IS_ACTIVE
  FROM DUAL) B
ON (A.KF = B.KF)
WHEN NOT MATCHED THEN 
INSERT (
  KF, BRANCH_NAME, IS_INTERNAL, SERVICE_BASE_URL, SERVICE_AUTH, 
  IS_ACTIVE)
VALUES (
  B.KF, B.BRANCH_NAME, B.IS_INTERNAL, B.SERVICE_BASE_URL, B.SERVICE_AUTH, 
  B.IS_ACTIVE)
WHEN MATCHED THEN
UPDATE SET 
  A.BRANCH_NAME = B.BRANCH_NAME,
  A.IS_INTERNAL = B.IS_INTERNAL,
  A.SERVICE_BASE_URL = B.SERVICE_BASE_URL,
  A.SERVICE_AUTH = B.SERVICE_AUTH,
  A.IS_ACTIVE = B.IS_ACTIVE;

MERGE INTO BARS.NBU_BRANCH_601 A USING
 (SELECT
  '323475' as KF,
  'Кіровоградське РУ' as BRANCH_NAME,
  0 as IS_INTERNAL,
  '*https://srvf-10-web01.oschadbank.ua/barsroot/' as SERVICE_BASE_URL,
  NULL as SERVICE_AUTH,
  0 as IS_ACTIVE
  FROM DUAL) B
ON (A.KF = B.KF)
WHEN NOT MATCHED THEN 
INSERT (
  KF, BRANCH_NAME, IS_INTERNAL, SERVICE_BASE_URL, SERVICE_AUTH, 
  IS_ACTIVE)
VALUES (
  B.KF, B.BRANCH_NAME, B.IS_INTERNAL, B.SERVICE_BASE_URL, B.SERVICE_AUTH, 
  B.IS_ACTIVE)
WHEN MATCHED THEN
UPDATE SET 
  A.BRANCH_NAME = B.BRANCH_NAME,
  A.IS_INTERNAL = B.IS_INTERNAL,
  A.SERVICE_BASE_URL = B.SERVICE_BASE_URL,
  A.SERVICE_AUTH = B.SERVICE_AUTH,
  A.IS_ACTIVE = B.IS_ACTIVE;

MERGE INTO BARS.NBU_BRANCH_601 A USING
 (SELECT
  '353553' as KF,
  'Чернігівське РУ' as BRANCH_NAME,
  0 as IS_INTERNAL,
  '*https://web-cngv.ho.obu/barsroot/' as SERVICE_BASE_URL,
  NULL as SERVICE_AUTH,
  0 as IS_ACTIVE
  FROM DUAL) B
ON (A.KF = B.KF)
WHEN NOT MATCHED THEN 
INSERT (
  KF, BRANCH_NAME, IS_INTERNAL, SERVICE_BASE_URL, SERVICE_AUTH, 
  IS_ACTIVE)
VALUES (
  B.KF, B.BRANCH_NAME, B.IS_INTERNAL, B.SERVICE_BASE_URL, B.SERVICE_AUTH, 
  B.IS_ACTIVE)
WHEN MATCHED THEN
UPDATE SET 
  A.BRANCH_NAME = B.BRANCH_NAME,
  A.IS_INTERNAL = B.IS_INTERNAL,
  A.SERVICE_BASE_URL = B.SERVICE_BASE_URL,
  A.SERVICE_AUTH = B.SERVICE_AUTH,
  A.IS_ACTIVE = B.IS_ACTIVE;

MERGE INTO BARS.NBU_BRANCH_601 A USING
 (SELECT
  '312356' as KF,
  'Закарпатське РУ' as BRANCH_NAME,
  0 as IS_INTERNAL,
  '*https://zakweb.zakarpat.obu/barsroot/' as SERVICE_BASE_URL,
  NULL as SERVICE_AUTH,
  0 as IS_ACTIVE
  FROM DUAL) B
ON (A.KF = B.KF)
WHEN NOT MATCHED THEN 
INSERT (
  KF, BRANCH_NAME, IS_INTERNAL, SERVICE_BASE_URL, SERVICE_AUTH, 
  IS_ACTIVE)
VALUES (
  B.KF, B.BRANCH_NAME, B.IS_INTERNAL, B.SERVICE_BASE_URL, B.SERVICE_AUTH, 
  B.IS_ACTIVE)
WHEN MATCHED THEN
UPDATE SET 
  A.BRANCH_NAME = B.BRANCH_NAME,
  A.IS_INTERNAL = B.IS_INTERNAL,
  A.SERVICE_BASE_URL = B.SERVICE_BASE_URL,
  A.SERVICE_AUTH = B.SERVICE_AUTH,
  A.IS_ACTIVE = B.IS_ACTIVE;

MERGE INTO BARS.NBU_BRANCH_601 A USING
 (SELECT
  '302076' as KF,
  'Вінницьке РУ' as BRANCH_NAME,
  0 as IS_INTERNAL,
  '*https://web-vinn.oschadbank.ua/barsroot/' as SERVICE_BASE_URL,
  NULL as SERVICE_AUTH,
  0 as IS_ACTIVE
  FROM DUAL) B
ON (A.KF = B.KF)
WHEN NOT MATCHED THEN 
INSERT (
  KF, BRANCH_NAME, IS_INTERNAL, SERVICE_BASE_URL, SERVICE_AUTH, 
  IS_ACTIVE)
VALUES (
  B.KF, B.BRANCH_NAME, B.IS_INTERNAL, B.SERVICE_BASE_URL, B.SERVICE_AUTH, 
  B.IS_ACTIVE)
WHEN MATCHED THEN
UPDATE SET 
  A.BRANCH_NAME = B.BRANCH_NAME,
  A.IS_INTERNAL = B.IS_INTERNAL,
  A.SERVICE_BASE_URL = B.SERVICE_BASE_URL,
  A.SERVICE_AUTH = B.SERVICE_AUTH,
  A.IS_ACTIVE = B.IS_ACTIVE;

MERGE INTO BARS.NBU_BRANCH_601 A USING
 (SELECT
  '328845' as KF,
  'Одеське РУ' as BRANCH_NAME,
  0 as IS_INTERNAL,
  '*https://web-odes.ho.obu/barsroot/' as SERVICE_BASE_URL,
  NULL as SERVICE_AUTH,
  0 as IS_ACTIVE
  FROM DUAL) B
ON (A.KF = B.KF)
WHEN NOT MATCHED THEN 
INSERT (
  KF, BRANCH_NAME, IS_INTERNAL, SERVICE_BASE_URL, SERVICE_AUTH, 
  IS_ACTIVE)
VALUES (
  B.KF, B.BRANCH_NAME, B.IS_INTERNAL, B.SERVICE_BASE_URL, B.SERVICE_AUTH, 
  B.IS_ACTIVE)
WHEN MATCHED THEN
UPDATE SET 
  A.BRANCH_NAME = B.BRANCH_NAME,
  A.IS_INTERNAL = B.IS_INTERNAL,
  A.SERVICE_BASE_URL = B.SERVICE_BASE_URL,
  A.SERVICE_AUTH = B.SERVICE_AUTH,
  A.IS_ACTIVE = B.IS_ACTIVE;

MERGE INTO BARS.NBU_BRANCH_601 A USING
 (SELECT
  '335106' as KF,
  'Донецьке РУ' as BRANCH_NAME,
  1 as IS_INTERNAL,
  '*https://web-dnck.ho.obu/barsroot/' as SERVICE_BASE_URL,
  NULL as SERVICE_AUTH,
  1 as IS_ACTIVE
  FROM DUAL) B
ON (A.KF = B.KF)
WHEN NOT MATCHED THEN 
INSERT (
  KF, BRANCH_NAME, IS_INTERNAL, SERVICE_BASE_URL, SERVICE_AUTH, 
  IS_ACTIVE)
VALUES (
  B.KF, B.BRANCH_NAME, B.IS_INTERNAL, B.SERVICE_BASE_URL, B.SERVICE_AUTH, 
  B.IS_ACTIVE)
WHEN MATCHED THEN
UPDATE SET 
  A.BRANCH_NAME = B.BRANCH_NAME,
  A.IS_INTERNAL = B.IS_INTERNAL,
  A.SERVICE_BASE_URL = B.SERVICE_BASE_URL,
  A.SERVICE_AUTH = B.SERVICE_AUTH,
  A.IS_ACTIVE = B.IS_ACTIVE;

MERGE INTO BARS.NBU_BRANCH_601 A USING
 (SELECT
  '311647' as KF,
  'Житомирське РУ' as BRANCH_NAME,
  0 as IS_INTERNAL,
  '*https://web05.oschadbank.ua/barsroot/' as SERVICE_BASE_URL,
  NULL as SERVICE_AUTH,
  0 as IS_ACTIVE
  FROM DUAL) B
ON (A.KF = B.KF)
WHEN NOT MATCHED THEN 
INSERT (
  KF, BRANCH_NAME, IS_INTERNAL, SERVICE_BASE_URL, SERVICE_AUTH, 
  IS_ACTIVE)
VALUES (
  B.KF, B.BRANCH_NAME, B.IS_INTERNAL, B.SERVICE_BASE_URL, B.SERVICE_AUTH, 
  B.IS_ACTIVE)
WHEN MATCHED THEN
UPDATE SET 
  A.BRANCH_NAME = B.BRANCH_NAME,
  A.IS_INTERNAL = B.IS_INTERNAL,
  A.SERVICE_BASE_URL = B.SERVICE_BASE_URL,
  A.SERVICE_AUTH = B.SERVICE_AUTH,
  A.IS_ACTIVE = B.IS_ACTIVE;

MERGE INTO BARS.NBU_BRANCH_601 A USING
 (SELECT
  '352457' as KF,
  'Херсонське РУ' as BRANCH_NAME,
  0 as IS_INTERNAL,
  '*https://web-khrsn.ho.obu/barsroot/' as SERVICE_BASE_URL,
  NULL as SERVICE_AUTH,
  0 as IS_ACTIVE
  FROM DUAL) B
ON (A.KF = B.KF)
WHEN NOT MATCHED THEN 
INSERT (
  KF, BRANCH_NAME, IS_INTERNAL, SERVICE_BASE_URL, SERVICE_AUTH, 
  IS_ACTIVE)
VALUES (
  B.KF, B.BRANCH_NAME, B.IS_INTERNAL, B.SERVICE_BASE_URL, B.SERVICE_AUTH, 
  B.IS_ACTIVE)
WHEN MATCHED THEN
UPDATE SET 
  A.BRANCH_NAME = B.BRANCH_NAME,
  A.IS_INTERNAL = B.IS_INTERNAL,
  A.SERVICE_BASE_URL = B.SERVICE_BASE_URL,
  A.SERVICE_AUTH = B.SERVICE_AUTH,
  A.IS_ACTIVE = B.IS_ACTIVE;

MERGE INTO BARS.NBU_BRANCH_601 A USING
 (SELECT
  '333368' as KF,
  'Рівненське РУ' as BRANCH_NAME,
  0 as IS_INTERNAL,
  '*https://web-rivne.ho.obu/barsroot/' as SERVICE_BASE_URL,
  NULL as SERVICE_AUTH,
  0 as IS_ACTIVE
  FROM DUAL) B
ON (A.KF = B.KF)
WHEN NOT MATCHED THEN 
INSERT (
  KF, BRANCH_NAME, IS_INTERNAL, SERVICE_BASE_URL, SERVICE_AUTH, 
  IS_ACTIVE)
VALUES (
  B.KF, B.BRANCH_NAME, B.IS_INTERNAL, B.SERVICE_BASE_URL, B.SERVICE_AUTH, 
  B.IS_ACTIVE)
WHEN MATCHED THEN
UPDATE SET 
  A.BRANCH_NAME = B.BRANCH_NAME,
  A.IS_INTERNAL = B.IS_INTERNAL,
  A.SERVICE_BASE_URL = B.SERVICE_BASE_URL,
  A.SERVICE_AUTH = B.SERVICE_AUTH,
  A.IS_ACTIVE = B.IS_ACTIVE;

MERGE INTO BARS.NBU_BRANCH_601 A USING
 (SELECT
  '325796' as KF,
  'Львівське РУ' as BRANCH_NAME,
  0 as IS_INTERNAL,
  '*https://web.lviv.obu/barsroot/' as SERVICE_BASE_URL,
  NULL as SERVICE_AUTH,
  0 as IS_ACTIVE
  FROM DUAL) B
ON (A.KF = B.KF)
WHEN NOT MATCHED THEN 
INSERT (
  KF, BRANCH_NAME, IS_INTERNAL, SERVICE_BASE_URL, SERVICE_AUTH, 
  IS_ACTIVE)
VALUES (
  B.KF, B.BRANCH_NAME, B.IS_INTERNAL, B.SERVICE_BASE_URL, B.SERVICE_AUTH, 
  B.IS_ACTIVE)
WHEN MATCHED THEN
UPDATE SET 
  A.BRANCH_NAME = B.BRANCH_NAME,
  A.IS_INTERNAL = B.IS_INTERNAL,
  A.SERVICE_BASE_URL = B.SERVICE_BASE_URL,
  A.SERVICE_AUTH = B.SERVICE_AUTH,
  A.IS_ACTIVE = B.IS_ACTIVE;

MERGE INTO BARS.NBU_BRANCH_601 A USING
 (SELECT
  '313957' as KF,
  'Запорізьке РУ' as BRANCH_NAME,
  0 as IS_INTERNAL,
  '*https://web-zprz.ho.obu/barsroot/' as SERVICE_BASE_URL,
  NULL as SERVICE_AUTH,
  0 as IS_ACTIVE
  FROM DUAL) B
ON (A.KF = B.KF)
WHEN NOT MATCHED THEN 
INSERT (
  KF, BRANCH_NAME, IS_INTERNAL, SERVICE_BASE_URL, SERVICE_AUTH, 
  IS_ACTIVE)
VALUES (
  B.KF, B.BRANCH_NAME, B.IS_INTERNAL, B.SERVICE_BASE_URL, B.SERVICE_AUTH, 
  B.IS_ACTIVE)
WHEN MATCHED THEN
UPDATE SET 
  A.BRANCH_NAME = B.BRANCH_NAME,
  A.IS_INTERNAL = B.IS_INTERNAL,
  A.SERVICE_BASE_URL = B.SERVICE_BASE_URL,
  A.SERVICE_AUTH = B.SERVICE_AUTH,
  A.IS_ACTIVE = B.IS_ACTIVE;

MERGE INTO BARS.NBU_BRANCH_601 A USING
 (SELECT
  '336503' as KF,
  'Івано-Франківське РУ' as BRANCH_NAME,
  0 as IS_INTERNAL,
  '*https://web-08-00.oschadbank.ua/barsroot/' as SERVICE_BASE_URL,
  NULL as SERVICE_AUTH,
  0 as IS_ACTIVE
  FROM DUAL) B
ON (A.KF = B.KF)
WHEN NOT MATCHED THEN 
INSERT (
  KF, BRANCH_NAME, IS_INTERNAL, SERVICE_BASE_URL, SERVICE_AUTH, 
  IS_ACTIVE)
VALUES (
  B.KF, B.BRANCH_NAME, B.IS_INTERNAL, B.SERVICE_BASE_URL, B.SERVICE_AUTH, 
  B.IS_ACTIVE)
WHEN MATCHED THEN
UPDATE SET 
  A.BRANCH_NAME = B.BRANCH_NAME,
  A.IS_INTERNAL = B.IS_INTERNAL,
  A.SERVICE_BASE_URL = B.SERVICE_BASE_URL,
  A.SERVICE_AUTH = B.SERVICE_AUTH,
  A.IS_ACTIVE = B.IS_ACTIVE;

MERGE INTO BARS.NBU_BRANCH_601 A USING
 (SELECT
  '303398' as KF,
  'Волинське РУ' as BRANCH_NAME,
  0 as IS_INTERNAL,
  '*https://web-volyn.oschadbank.ua/barsroot/' as SERVICE_BASE_URL,
  NULL as SERVICE_AUTH,
  0 as IS_ACTIVE
  FROM DUAL) B
ON (A.KF = B.KF)
WHEN NOT MATCHED THEN 
INSERT (
  KF, BRANCH_NAME, IS_INTERNAL, SERVICE_BASE_URL, SERVICE_AUTH, 
  IS_ACTIVE)
VALUES (
  B.KF, B.BRANCH_NAME, B.IS_INTERNAL, B.SERVICE_BASE_URL, B.SERVICE_AUTH, 
  B.IS_ACTIVE)
WHEN MATCHED THEN
UPDATE SET 
  A.BRANCH_NAME = B.BRANCH_NAME,
  A.IS_INTERNAL = B.IS_INTERNAL,
  A.SERVICE_BASE_URL = B.SERVICE_BASE_URL,
  A.SERVICE_AUTH = B.SERVICE_AUTH,
  A.IS_ACTIVE = B.IS_ACTIVE;

MERGE INTO BARS.NBU_BRANCH_601 A USING
 (SELECT
  '331467' as KF,
  'Полтавське РУ' as BRANCH_NAME,
  0 as IS_INTERNAL,
  '*https://bars16.oschadbank.ua/barsroot/' as SERVICE_BASE_URL,
  NULL as SERVICE_AUTH,
  0 as IS_ACTIVE
  FROM DUAL) B
ON (A.KF = B.KF)
WHEN NOT MATCHED THEN 
INSERT (
  KF, BRANCH_NAME, IS_INTERNAL, SERVICE_BASE_URL, SERVICE_AUTH, 
  IS_ACTIVE)
VALUES (
  B.KF, B.BRANCH_NAME, B.IS_INTERNAL, B.SERVICE_BASE_URL, B.SERVICE_AUTH, 
  B.IS_ACTIVE)
WHEN MATCHED THEN
UPDATE SET 
  A.BRANCH_NAME = B.BRANCH_NAME,
  A.IS_INTERNAL = B.IS_INTERNAL,
  A.SERVICE_BASE_URL = B.SERVICE_BASE_URL,
  A.SERVICE_AUTH = B.SERVICE_AUTH,
  A.IS_ACTIVE = B.IS_ACTIVE;

MERGE INTO BARS.NBU_BRANCH_601 A USING
 (SELECT
  '351823' as KF,
  'Харківське РУ' as BRANCH_NAME,
  1 as IS_INTERNAL,
  '*https://mmfo-web.oschadbank.ua/barsroot/' as SERVICE_BASE_URL,
  NULL as SERVICE_AUTH,
  1 as IS_ACTIVE
  FROM DUAL) B
ON (A.KF = B.KF)
WHEN NOT MATCHED THEN 
INSERT (
  KF, BRANCH_NAME, IS_INTERNAL, SERVICE_BASE_URL, SERVICE_AUTH, 
  IS_ACTIVE)
VALUES (
  B.KF, B.BRANCH_NAME, B.IS_INTERNAL, B.SERVICE_BASE_URL, B.SERVICE_AUTH, 
  B.IS_ACTIVE)
WHEN MATCHED THEN
UPDATE SET 
  A.BRANCH_NAME = B.BRANCH_NAME,
  A.IS_INTERNAL = B.IS_INTERNAL,
  A.SERVICE_BASE_URL = B.SERVICE_BASE_URL,
  A.SERVICE_AUTH = B.SERVICE_AUTH,
  A.IS_ACTIVE = B.IS_ACTIVE;

MERGE INTO BARS.NBU_BRANCH_601 A USING
 (SELECT
  '337568' as KF,
  'Сумське РУ' as BRANCH_NAME,
  0 as IS_INTERNAL,
  '*https://web-sumy.ho.obu/barsroot/' as SERVICE_BASE_URL,
  NULL as SERVICE_AUTH,
  0 as IS_ACTIVE
  FROM DUAL) B
ON (A.KF = B.KF)
WHEN NOT MATCHED THEN 
INSERT (
  KF, BRANCH_NAME, IS_INTERNAL, SERVICE_BASE_URL, SERVICE_AUTH, 
  IS_ACTIVE)
VALUES (
  B.KF, B.BRANCH_NAME, B.IS_INTERNAL, B.SERVICE_BASE_URL, B.SERVICE_AUTH, 
  B.IS_ACTIVE)
WHEN MATCHED THEN
UPDATE SET 
  A.BRANCH_NAME = B.BRANCH_NAME,
  A.IS_INTERNAL = B.IS_INTERNAL,
  A.SERVICE_BASE_URL = B.SERVICE_BASE_URL,
  A.SERVICE_AUTH = B.SERVICE_AUTH,
  A.IS_ACTIVE = B.IS_ACTIVE;

MERGE INTO BARS.NBU_BRANCH_601 A USING
 (SELECT
  '315784' as KF,
  'Хмельницьке РУ' as BRANCH_NAME,
  0 as IS_INTERNAL,
  '*https://web-khml.oschadbank.ua/barsroot/' as SERVICE_BASE_URL,
  NULL as SERVICE_AUTH,
  0 as IS_ACTIVE
  FROM DUAL) B
ON (A.KF = B.KF)
WHEN NOT MATCHED THEN 
INSERT (
  KF, BRANCH_NAME, IS_INTERNAL, SERVICE_BASE_URL, SERVICE_AUTH, 
  IS_ACTIVE)
VALUES (
  B.KF, B.BRANCH_NAME, B.IS_INTERNAL, B.SERVICE_BASE_URL, B.SERVICE_AUTH, 
  B.IS_ACTIVE)
WHEN MATCHED THEN
UPDATE SET 
  A.BRANCH_NAME = B.BRANCH_NAME,
  A.IS_INTERNAL = B.IS_INTERNAL,
  A.SERVICE_BASE_URL = B.SERVICE_BASE_URL,
  A.SERVICE_AUTH = B.SERVICE_AUTH,
  A.IS_ACTIVE = B.IS_ACTIVE;

MERGE INTO BARS.NBU_BRANCH_601 A USING
 (SELECT
  '300465' as KF,
  'Правління' as BRANCH_NAME,
  1 as IS_INTERNAL,
  '*https://mmfo-web.oschadbank.ua/barsroot/' as SERVICE_BASE_URL,
  NULL as SERVICE_AUTH,
  1 as IS_ACTIVE
  FROM DUAL) B
ON (A.KF = B.KF)
WHEN NOT MATCHED THEN 
INSERT (
  KF, BRANCH_NAME, IS_INTERNAL, SERVICE_BASE_URL, SERVICE_AUTH, 
  IS_ACTIVE)
VALUES (
  B.KF, B.BRANCH_NAME, B.IS_INTERNAL, B.SERVICE_BASE_URL, B.SERVICE_AUTH, 
  B.IS_ACTIVE)
WHEN MATCHED THEN
UPDATE SET 
  A.BRANCH_NAME = B.BRANCH_NAME,
  A.IS_INTERNAL = B.IS_INTERNAL,
  A.SERVICE_BASE_URL = B.SERVICE_BASE_URL,
  A.SERVICE_AUTH = B.SERVICE_AUTH,
  A.IS_ACTIVE = B.IS_ACTIVE;

COMMIT;
end;
/

set define on

