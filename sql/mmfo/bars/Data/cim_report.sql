begin
MERGE INTO BARS.CIM_REPORTS_LIST A USING
 (SELECT
  16 as REPORT_ID,
  'Реєстр ММ за період' as NAME,
  NULL as PROC,
  'register_md_mm.frx' as TEMPLATE,
  'excel' as FILE_TYPE,
  'register_md.xlsx' as FILE_NAME,
  0 as USE_IN_TVBV
  FROM DUAL) B
ON (A.REPORT_ID = B.REPORT_ID)
WHEN NOT MATCHED THEN 
INSERT (
  REPORT_ID, NAME, PROC, TEMPLATE, FILE_TYPE, 
  FILE_NAME, USE_IN_TVBV)
VALUES (
  B.REPORT_ID, B.NAME, B.PROC, B.TEMPLATE, B.FILE_TYPE, 
  B.FILE_NAME, B.USE_IN_TVBV)
WHEN MATCHED THEN
UPDATE SET 
  A.NAME = B.NAME,
  A.PROC = B.PROC,
  A.TEMPLATE = B.TEMPLATE,
  A.FILE_TYPE = B.FILE_TYPE,
  A.FILE_NAME = B.FILE_NAME,
  A.USE_IN_TVBV = B.USE_IN_TVBV;

MERGE INTO BARS.CIM_REPORT_PARAMS A USING
 (SELECT
  16 as REPORT_ID,
  0 as PARAM_ID,
  'З' as NAME,
  'Date' as TYPE_ID,
  'p_fdat1' as PARAM_NAME,
  'bankdate-7' as DEFAULT_VALUE,
  1 as REQUIRED
  FROM DUAL) B
ON (A.REPORT_ID = B.REPORT_ID and A.PARAM_ID = B.PARAM_ID)
WHEN NOT MATCHED THEN 
INSERT (
  REPORT_ID, PARAM_ID, NAME, TYPE_ID, PARAM_NAME, 
  DEFAULT_VALUE, REQUIRED)
VALUES (
  B.REPORT_ID, B.PARAM_ID, B.NAME, B.TYPE_ID, B.PARAM_NAME, 
  B.DEFAULT_VALUE, B.REQUIRED)
WHEN MATCHED THEN
UPDATE SET 
  A.NAME = B.NAME,
  A.TYPE_ID = B.TYPE_ID,
  A.PARAM_NAME = B.PARAM_NAME,
  A.DEFAULT_VALUE = B.DEFAULT_VALUE,
  A.REQUIRED = B.REQUIRED;

MERGE INTO BARS.CIM_REPORT_PARAMS A USING
 (SELECT
  16 as REPORT_ID,
  1 as PARAM_ID,
  'По' as NAME,
  'Date' as TYPE_ID,
  'p_fdat2' as PARAM_NAME,
  'bankdate' as DEFAULT_VALUE,
  1 as REQUIRED
  FROM DUAL) B
ON (A.REPORT_ID = B.REPORT_ID and A.PARAM_ID = B.PARAM_ID)
WHEN NOT MATCHED THEN 
INSERT (
  REPORT_ID, PARAM_ID, NAME, TYPE_ID, PARAM_NAME, 
  DEFAULT_VALUE, REQUIRED)
VALUES (
  B.REPORT_ID, B.PARAM_ID, B.NAME, B.TYPE_ID, B.PARAM_NAME, 
  B.DEFAULT_VALUE, B.REQUIRED)
WHEN MATCHED THEN
UPDATE SET 
  A.NAME = B.NAME,
  A.TYPE_ID = B.TYPE_ID,
  A.PARAM_NAME = B.PARAM_NAME,
  A.DEFAULT_VALUE = B.DEFAULT_VALUE,
  A.REQUIRED = B.REQUIRED;

MERGE INTO BARS.CIM_REPORT_PARAMS A USING
 (SELECT
  16 as REPORT_ID,
  2 as PARAM_ID,
  'Назва резидента' as NAME,
  'Varchar2' as TYPE_ID,
  'p_name' as PARAM_NAME,
  NULL as DEFAULT_VALUE,
  NULL as REQUIRED
  FROM DUAL) B
ON (A.REPORT_ID = B.REPORT_ID and A.PARAM_ID = B.PARAM_ID)
WHEN NOT MATCHED THEN 
INSERT (
  REPORT_ID, PARAM_ID, NAME, TYPE_ID, PARAM_NAME, 
  DEFAULT_VALUE, REQUIRED)
VALUES (
  B.REPORT_ID, B.PARAM_ID, B.NAME, B.TYPE_ID, B.PARAM_NAME, 
  B.DEFAULT_VALUE, B.REQUIRED)
WHEN MATCHED THEN
UPDATE SET 
  A.NAME = B.NAME,
  A.TYPE_ID = B.TYPE_ID,
  A.PARAM_NAME = B.PARAM_NAME,
  A.DEFAULT_VALUE = B.DEFAULT_VALUE,
  A.REQUIRED = B.REQUIRED;

MERGE INTO BARS.CIM_REPORT_PARAMS A USING
 (SELECT
  16 as REPORT_ID,
  3 as PARAM_ID,
  'ОКПО резидента' as NAME,
  'Varchar2' as TYPE_ID,
  'p_okpo' as PARAM_NAME,
  NULL as DEFAULT_VALUE,
  NULL as REQUIRED
  FROM DUAL) B
ON (A.REPORT_ID = B.REPORT_ID and A.PARAM_ID = B.PARAM_ID)
WHEN NOT MATCHED THEN 
INSERT (
  REPORT_ID, PARAM_ID, NAME, TYPE_ID, PARAM_NAME, 
  DEFAULT_VALUE, REQUIRED)
VALUES (
  B.REPORT_ID, B.PARAM_ID, B.NAME, B.TYPE_ID, B.PARAM_NAME, 
  B.DEFAULT_VALUE, B.REQUIRED)
WHEN MATCHED THEN
UPDATE SET 
  A.NAME = B.NAME,
  A.TYPE_ID = B.TYPE_ID,
  A.PARAM_NAME = B.PARAM_NAME,
  A.DEFAULT_VALUE = B.DEFAULT_VALUE,
  A.REQUIRED = B.REQUIRED;

MERGE INTO BARS.CIM_REPORT_PARAMS A USING
 (SELECT
  16 as REPORT_ID,
  4 as PARAM_ID,
  'Тип МД' as NAME,
  'Varchar2' as TYPE_ID,
  'p_mdtype' as PARAM_NAME,
  NULL as DEFAULT_VALUE,
  NULL as REQUIRED
  FROM DUAL) B
ON (A.REPORT_ID = B.REPORT_ID and A.PARAM_ID = B.PARAM_ID)
WHEN NOT MATCHED THEN 
INSERT (
  REPORT_ID, PARAM_ID, NAME, TYPE_ID, PARAM_NAME, 
  DEFAULT_VALUE, REQUIRED)
VALUES (
  B.REPORT_ID, B.PARAM_ID, B.NAME, B.TYPE_ID, B.PARAM_NAME, 
  B.DEFAULT_VALUE, B.REQUIRED)
WHEN MATCHED THEN
UPDATE SET 
  A.NAME = B.NAME,
  A.TYPE_ID = B.TYPE_ID,
  A.PARAM_NAME = B.PARAM_NAME,
  A.DEFAULT_VALUE = B.DEFAULT_VALUE,
  A.REQUIRED = B.REQUIRED;

MERGE INTO BARS.CIM_REPORT_PARAMS A USING
 (SELECT
  16 as REPORT_ID,
  5 as PARAM_ID,
  'Код валюти' as NAME,
  'Varchar2' as TYPE_ID,
  'p_kv' as PARAM_NAME,
  NULL as DEFAULT_VALUE,
  NULL as REQUIRED
  FROM DUAL) B
ON (A.REPORT_ID = B.REPORT_ID and A.PARAM_ID = B.PARAM_ID)
WHEN NOT MATCHED THEN 
INSERT (
  REPORT_ID, PARAM_ID, NAME, TYPE_ID, PARAM_NAME, 
  DEFAULT_VALUE, REQUIRED)
VALUES (
  B.REPORT_ID, B.PARAM_ID, B.NAME, B.TYPE_ID, B.PARAM_NAME, 
  B.DEFAULT_VALUE, B.REQUIRED)
WHEN MATCHED THEN
UPDATE SET 
  A.NAME = B.NAME,
  A.TYPE_ID = B.TYPE_ID,
  A.PARAM_NAME = B.PARAM_NAME,
  A.DEFAULT_VALUE = B.DEFAULT_VALUE,
  A.REQUIRED = B.REQUIRED;

MERGE INTO BARS.CIM_REPORT_PARAMS A USING
 (SELECT
  16 as REPORT_ID,
  6 as PARAM_ID,
  'Код установи' as NAME,
  'Varchar2' as TYPE_ID,
  'p_branch' as PARAM_NAME,
  NULL as DEFAULT_VALUE,
  NULL as REQUIRED
  FROM DUAL) B
ON (A.REPORT_ID = B.REPORT_ID and A.PARAM_ID = B.PARAM_ID)
WHEN NOT MATCHED THEN 
INSERT (
  REPORT_ID, PARAM_ID, NAME, TYPE_ID, PARAM_NAME, 
  DEFAULT_VALUE, REQUIRED)
VALUES (
  B.REPORT_ID, B.PARAM_ID, B.NAME, B.TYPE_ID, B.PARAM_NAME, 
  B.DEFAULT_VALUE, B.REQUIRED)
WHEN MATCHED THEN
UPDATE SET 
  A.NAME = B.NAME,
  A.TYPE_ID = B.TYPE_ID,
  A.PARAM_NAME = B.PARAM_NAME,
  A.DEFAULT_VALUE = B.DEFAULT_VALUE,
  A.REQUIRED = B.REQUIRED;
end;
/
COMMIT;
